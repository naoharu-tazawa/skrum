本日の所属グループ進捗状況レポートをお知らせします。


{% for group in data %}

■ {{ group.groupName }}
    (  前日  )    ( 前々日 )    (前々日比)
      {{ group.achievementRateToday }}%        {{ group.achievementRateYesterday }}%        {% if (group.achievementRateToday - group.achievementRateYesterday) >= 0 %}+{% else %}-{% endif %}{{ group.achievementRateToday - group.achievementRateYesterday }}%

{% endfor %}
{% if data == null %}◯本日のレポート対象グループはありません。{% endif %}





※このメールが不要な方は以下の方法で設定できます。
 1. 下記URLからログインをしてください。
 2. ヘッダーの一番右のユーザ画像ボタンを押して、「設定」ボタンを押してください。
 3. サイドバーの「メール配信」タブを押してください。
 4. 「グループ進捗状況レポートメール」を「受信しない」に設定し、「保存する」ボタンを押してください。
 5. 以上で設定完了です。
 https://{{ subdomain }}.skrum.jp/login
