フィードバック対象者自動抽出レポートをお知らせします。

以下に抽出された社員の方にはフィードバックが必要な可能性があります。
フィードバックの要否を検討し必要に応じてフィードバックやアドバイスを行ってください。


{% for group in data %}

■ {{ group.groupName }}

  {% for member in group.members %}
      {{ member.memberName }}
            ：{{ member.reason }}
  {% endfor %}

{% endfor %}
{% if data == null %}◯本日のレポート対象者はおりません。{% endif %}





 ※このメールが不要な方は以下の方法で設定できます。
 1. 下記URLからログインをしてください。
 2. ヘッダーの一番右のユーザ画像ボタンを押して、「設定」ボタンを押してください。
 3. サイドバーの「メール配信」タブを押してください。
 4. 「フィードバック対象者通知メール」を「受信しない」に設定し、「保存する」ボタンを押してください。
 5. 以上で設定完了です。
 https://{{ subdomain }}.skrum.jp/login
