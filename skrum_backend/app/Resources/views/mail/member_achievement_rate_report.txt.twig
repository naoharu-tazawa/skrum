本日のメンバー進捗状況レポートをお知らせします。


{% for group in data %}

■ {{ group.groupName }}
    (  前日  )    ( 前々日 )    (前々日比)
      {{ group.achievementRateToday }}%        {{ group.achievementRateYesterday }}%        {% if (group.achievementRateToday - group.achievementRateYesterday) >= 0 %}+{% else %}-{% endif %}{{ group.achievementRateToday - group.achievementRateYesterday }}%

              (  前日  )    ( 前々日 )    (前々日比)
  {% for member in group.members %}
      {{ member.memberName }}
                {{ member.achievementRateToday }}%        {{ member.achievementRateYesterday }}%        {% if (member.achievementRateToday - member.achievementRateYesterday) >= 0 %}+{% else %}-{% endif %}{{ member.achievementRateToday - member.achievementRateYesterday }}%
  {% endfor %}

{% endfor %}
{% if data == null %}◯本日のレポート対象者はおりません。{% endif %}





※このメールが不要な方は以下の方法で設定できます。
 1. 下記URLからログインをしてください。
 2. ヘッダーの一番右のユーザ画像ボタンを押して、「設定」ボタンを押してください。
 3. サイドバーの「メール配信」タブを押してください。
 4. 「メンバー進捗状況レポートメール」を「受信しない」に設定し、「保存する」ボタンを押してください。
 5. 以上で設定完了です。
 https://{{ subdomain }}.skrum.jp/login
